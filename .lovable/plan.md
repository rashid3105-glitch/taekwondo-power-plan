# Coach invite-system — udvider eksisterende godkendelsesflow\n\nAppen har allerede:\n- `profiles.is_approved` + `/admin/approval` med klubgruppering, godkend/afvis, e-mail-notifikationer\n- `clubs` (med `name`), `coach_athletes` (coach↔atlet binding), `user_roles` ('admin','coach')\n- Onboarding der låser uapprovede brugere ude\n\nDenne plan **udvider** det — den **erstatter intet**. Ingen nye `team_members` tabel (vi genbruger `profiles.is_approved` + `coach_athletes`). Ingen `is_admin`-kolonne (admin = `user_roles`).\n\n---\n\n## 1. Database — én ny tabel\n\n`coach_invites`:\n- `id`,