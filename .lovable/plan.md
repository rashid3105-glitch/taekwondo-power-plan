Swap the visual styling between the "Testning" and "I dag" buttons in the coach quick-access grid in `src/components/hub/AthleteDashboard.tsx`.

- "I dag" becomes the filled accent (yellow/blue) button
- "Testning" becomes the outlined dark button
- Keep layout unchanged: `[Testning] [Video-analyse]` top row, `[I dag]` full-width below
- No other changes