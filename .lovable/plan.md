\n# First-Run Onboarding Wizard\n\n## What we're building\n\nA new role-branched first-run onboarding flow at `/onboarding`. Triggered automatically the first time a user lands on `/dashboard` (or `/coach`) without `onboarding_completed = true` on their profile. Once completed, never shown again. Existing `/profile-setup` stays as the deep-edit settings page (untouched in routing or fields).\n\nAll strings go through the existing `t()` i18n system across DA/EN/SV/DE/NO/AR. Dark cockpit theme + existing shadcn primitives. Mobile-first.\n\n## Validation against codebase\n\n- **Belt levels** → keep existing 6 colors (`white/yellow/green/blue/red/black`) with localized Danish labels. No schema change.\n- **Existing ProfileSetup** → leave fully intact. Onboarding writes the same columns via the same `update-my-profile` edge function so data stays consistent.\n- **Coach detection** → \